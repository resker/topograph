# Default values for topology-state-observer.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/nvidia/topograph
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

rbac:
  # Specifies whether RBAC resources should be created for the service account
  create: true

verbosity: 3

# Additional environment variables for the node-observer container.
env: {}

# Additional init containers for the node-observer Pod.
initContainers: []

# Optional lifecycle hooks for the node-observer container.
lifecycle: {}

topograph:
  # Watches the Topograph API pod and triggers topology generation when the
  # API becomes Ready after startup or a container restart.
  apiServer:
    enabled: true
    # Leave podSelector empty to use the chart's release-scoped API pod labels.
    podSelector: {}
    containerName: topograph
  # Waits for node-data-broker DaemonSet pods to become Ready before the first
  # topology request so node annotations are applied first. Disable when the
  # node-data-broker subchart is not deployed (node-data-broker.enabled: false).
  nodeDataBroker:
    enabled: true
    # Leave podSelector empty to use the chart's release-scoped broker pod labels.
    podSelector: {}
    containerName: node-data-broker
  # Initiate topology request based on changes in selected nodes and pods
  trigger:
#    nodeSelector: {}
#    podSelector: {}

podAnnotations: {}
podLabels: {}

# Hardened by default to satisfy the Kubernetes "restricted" Pod Security
# Standard. The node-observer only watches nodes/pods and calls the Topograph
# API, so it needs no elevated privileges or filesystem writes.
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  fsGroup: 65532
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  capabilities:
    drop:
      - ALL

resources:
  limits:
    cpu: 400m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

nodeSelector: {}

tolerations: []

affinity: {}
